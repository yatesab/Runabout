using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RadarScreen : MonoBehaviour
{

    public ShipRadar shipRadar;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(shipRadar.TriggerList.Count > 0)
        // {
        //     foreach(var trigger in shipRadar.TriggerList)
        //     {
        //         Debug.Log(trigger.transform.position);
        //     }
        // }
    }
}
